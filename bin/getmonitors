#! /bin/sh

if [ ! -z "$(neofetch | grep i3)" ]; then
    echo "here"
    xrandr | grep -w connected
elif [ ! -z "$(neofetch | grep sway)" ]; then
    swaymsg -t get_outputs | grep name | sed 's/"/\n/g' | grep DP
else 
    echo "This commands does not work on any other WM than i3 & sway"
fi
