#! /bin/sh
clear
rm sample.out 2>/dev/null
g++ -std=c++20 -fmodules-ts -o sample.out $1 2>/dev/null
if [ ! -f "sample.out" ]; then
#     g++ -o sample.out $1
    g++ -std=c++20 -fmodules-ts -o sample.out $1
fi
./sample.out
